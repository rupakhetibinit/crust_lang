fn main() -> u64 {
    let factorial : fn(u64) -> u64 = |fact : u64| {
        if (fact == 0 || fact == 1) {
            1
        } else {
            fact * factorial(fact - 1)
        }
    };

    return factorial(10);
}
